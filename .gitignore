# =================================================
# ===        קובץ התעלמות עבור Git         ===
# =================================================

# קבצים רגישים - בשום אופן לא להעלות ל-GitHub!
# קובץ זה מכיל את הסיסמאות ופרטי ההתחברות למסד הנתונים.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# תלויות וספריות צד-שלישי
# תיקייה זו נוצרת אוטומטית על ידי 'npm install' והיא כבדה מאוד.
# אין צורך לגבות אותה, כל מפתח יכול ליצור אותה מחדש.
/node_modules

# תוצרי בנייה (Build output)
# תיקייה זו נוצרת על ידי 'npm run build' ומכילה את גרסת האתר המוכנה להעלאה.
# אין צורך לגבות אותה כי היא נוצרת מהקוד המקורי.
/dist
/build

# לוגים וקבצי שגיאות
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# קבצים ותיקיות של סביבות פיתוח (Editors)
.idea
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# קבצי מערכת של Mac ו-Windows
.DS_Store
Thumbs.db